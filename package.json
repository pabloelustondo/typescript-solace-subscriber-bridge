{
    "name": "typescript-solace-subscriber-bridge",
    "scripts": {
        "start": "ts-node startup.ts",
        "test": "ts-node ./src/InternalRetryQueue.ts"
    },
    "version": "0.0.0",
    "description": "Simple Solace Queue subscriber that will use a configuration file to map messages to url representing services using a stateless delayed queue strategy for message retry",
    "dependencies": {
        "axios": "^1.3.4",
        "solclientjs": "10.11.0"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/pabloelustondo/typescript-solace-subscriber-bridge/settings"
    },
    "devDependencies": {
        "@types/uuid": "^9.0.1",
        "ts-node": "10.9.1",
        "uuid": "^9.0.0"
    }
}
